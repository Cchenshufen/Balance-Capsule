# Quota Orb v1.2.2

## 修复

- 区分 CCS 的 `claude-desktop` 与 `claude` 当前供应商，避免 Claude Desktop 第三方 API 被误判为官方 Claude。
- 数据来源菜单拆分为 Claude Desktop 与 Claude Code CLI，并分别记忆选择。
- Claude Desktop 通过 CCS 接入 DeepSeek/OpenRouter 时，复用安全余额适配器显示剩余金额。
