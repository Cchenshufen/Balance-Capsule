# Quota Orb v1.2.5

## 修复

- 修复 CC Switch 关闭后，Codex 恢复为官方登录配置却被误判为“不支持”的问题。
- `custom/OpenAI/requires_openai_auth=true` 且无 Base URL 时恢复读取官方额度。
- 同一 Codex 进程此前已确认第三方来源时，仍由运行实例锁定优先，避免误读官方额度。

实现不读取进程内存、环境变量、会话正文或 CCS 请求日志。
