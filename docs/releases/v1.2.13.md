# Quota Orb v1.2.13

- macOS 版的今日、本月与总计 Token 改为读取 Codex 当前登录账号的官方 `account/usage/read` 响应。
- 删除对本机 `~/.codex/sessions` 与 `archived_sessions` 的 Token 扫描和缓存，不再用本地会话估算账号总量。
- 账号 Token 查询失败或数据延迟时仍保留已读取的额度，不让刷新过程清空额度。
- 切换到 Claude Code 时不混用 Codex Token；Claude 个人账号没有同等官方接口，组织账号需 Admin/Analytics API 凭据。
