# Quota Orb v1.2.4

## 稳定性

- 同一 Codex/Claude 进程仍运行时，CCS/Codex++ 退出不再自动回退并读取官方额度。
- CCS 供应商变为非当前后，仍可按上次已确认的供应商 ID 安全读取余额。
- Codex++ 凭据随退出被移除时保留上次成功余额并标记过期。
- 非 `openai` 的自定义供应商缺少 Base URL 时改为失败关闭，不再猜测为官方。

实现只比较 Agent 进程 ID，不读取进程内存、环境变量、会话正文或 CCS 请求日志。
